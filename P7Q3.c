#include <stdio.h>
void main()
{
    //Following are the different type of pointer used:
    printf("The size of int pointer = %d\n", sizeof(int *));
    printf("The size of char pointer = %d\n", sizeof(char *));
    printf("The size of float pointer = %d\n", sizeof(float *));
    printf("The size of double pointer = %d\n", sizeof(double *));
}